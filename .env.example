# ローカルで npm run dev / npm run preview を使ってログイン機能まで試したい場合のみ設定してください。
# このファイルを .env.local にコピーして値を入れてください（.env.local はgit管理外です）。
# 値はSupabaseダッシュボード、またはVercelプロジェクトの環境変数から確認できます。
SUPABASE_URL=
SUPABASE_KEY=
